# Copiar este archivo a .env y completar con los valores reales.
# El .env real NUNCA se commitea (está en .gitignore).

# --- Google Cloud ---
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_REGION=us-central1

# --- Vertex AI (Gemini) ---
VERTEX_AI_LOCATION=us-central1
GEMINI_MODEL_FLASH=gemini-3.5-flash
GEMINI_MODEL_PRO=gemini-3.5-pro

# --- Firestore ---
FIRESTORE_DATABASE_ID=(default)

# --- Pub/Sub ---
PUBSUB_TOPIC_PERFIL_REGISTRADO=perfil-registrado
PUBSUB_SUBSCRIPTION_MATCHING=matching-worker

# --- App ---
PORT=8080
UMBRAL_FIT_MINIMO=75

# --- MVP Local (opcional) ---
# Para desarrollo sin credenciales GCP, levanta el emulador de Firestore:
#   gcloud emulators firestore start --host-port=localhost:8080
# y exporta:
#   FIRESTORE_EMULATOR_HOST=localhost:8080
# (el SDK de Firestore lo detecta automáticamente)

# Nota: En el MVP, los embeddings y Vertex AI están DESHABILITADOS.
# El matching usa solapamiento de tokens determinista (ver backend.md Fase 4).
